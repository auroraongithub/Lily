# Import / Export (Phase 0)

- Heavy libs like `pdf-lib`, `pdfmake`, and EPUB generators must be lazy-loaded.
- See `features/import-export/pdf.ts` for an example with `pdf-lib`.
- UI for this feature is in `app/import-export/page.tsx`.
