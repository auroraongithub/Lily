# Themes (Phase 0)

This folder will contain theme definitions and utilities. Use CSS variables defined in `app/globals.css`.

- Add theme switchers/components locally to the consuming feature or page.
- Persist user theme preference in local storage or IndexedDB.
